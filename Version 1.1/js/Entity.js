class Entity {
    constructor() {
        //this.root = theRoot;
        this.domElement = document.createElement("img");
        this.domElement.style.position = 'absolute';
        this.domElement.style.zIndex = 5;
        // Add the rest later; test the concept now!
    }
};